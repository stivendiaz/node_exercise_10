extends layout.pug
block content
    a(href="/login") Salir
    
    table.table.table-bordered
        thead
            tr
                th Name
                th Email
        
        tbody
            each val in visitors
                tr
                    td #{val.name}
                    td #{val.email}

            